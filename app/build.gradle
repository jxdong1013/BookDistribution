apply plugin: 'com.android.application'

android {
    signingConfigs {
        jxdsigningconfig {
            keyAlias 'jxd'
            keyPassword 'jinxiangdong'
            storeFile file('E:/MyJob/Person/Project/BookDistribution/src/Signing/keystore')
            storePassword 'jinxiangdong'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "com.jxd.bookdistribution"
        minSdkVersion 10
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.jxdsigningconfig
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.jxdsigningconfig
        }
    }
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.2'
    compile files('libs/gson-2.2.4.jar')
    compile files('libs/guavalib.jar')
}
